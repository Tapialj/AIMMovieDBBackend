spring.application.name=aimmoviedb

spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USER}
spring.datasource.password=${DATASOURCE_PASSWORD}

spring.jpa.open-in-view=false

cors.url=${FRONTEND_URL},http://localhost:3030,http://localhost:3080

# spring.jackson.datatype.datetime.write-dates-as-timestamps=false

# rsa.private-key=classpath:certs/private.pem
# rsa.public-key=classpath:certs/public.pem

logging.level.root=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.file==%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.file.name=logs/app.log

# logging.level.dev.jptapia.tanktracker.service=DEBUG
# logging.level.dev.jptapia.tanktracker.controller=DEBUG
# logging.level.org.hibernate.type=TRACE
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# logging.level.org.hibernate.orm.jdbc.bind=TRACE
# logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
# logging.level.org.springframework.jdbc.core.StatementCreatorUtils=TRACE
# logging.level.org.springframework.security=TRACE

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.properties.hibernate.format_sql=true

# server.error.include-message=always
